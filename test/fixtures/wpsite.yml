base_dir: ~/wpsite-test-root
cloud_base: ~/wpsite-cloud
keep_backups: 3
clients:
  acme:
    ssh: ubuntu@acme.example
    wp_root: /var/www/acme
  baker:
    ssh: root@baker.example
    wp_root: /var/www/html
    local_host: baker-custom.test
    remote_tmp: ~/.wpsite_tmp
    cloud_dir: /Volumes/Drive/clients/baker-final.com
    keep_backups: 6
    deactivate_plugins:
      - some-prod-plugin

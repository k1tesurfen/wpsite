# Local config (contains server hostnames / paths). Anchored to repo root so the
# bare name doesn't also swallow test/fixtures/wpsite.yml (the bats config fixture,
# which MUST be committed or CI's config/apply/backup tests fail on a clean checkout).
/wpsite.yml
.config/
!test/fixtures/wpsite.yml

# macOS
.DS_Store

CLAUDE.md

# Assembled hand-off bundle (built .app copies + installer). Regenerate from dist/
# + the two `tauri build` outputs; too large / binary to commit.
/dist-bundle/
